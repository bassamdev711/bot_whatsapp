# تحقيق خطأ QR 401 في عامل Render

## الأدلة المؤكدة

| البيئة | النتيجة | الدليل |
|---|---|---|
| العامل المحلي مع جلسة جديدة | تولّد رمز QR بنجاح | استجابة `GET /v1/session` احتوت `qrDataUrl` أثناء اختبار المنفذ المحلي 3202. |
| عامل Render الحالي | يبدأ خادم HTTP ويبني المشروع بنجاح، ثم يغلق WhatsApp المصافحة بـ401 قبل QR | سجل Render للـcommit `e54e9b5`: `Wasla WhatsApp socket closed`, `code: 401`, `Connection Failure`. |
| كود الربط من Render | وصل إشعار طلب كود إلى هاتف بسّام مرة واحدة | أكد بسّام وصول إشعار على الهاتف، وسجل الجلسة احتوى حدث `session.pairing_code`. |
| واجهة Vercel | تتصل بالعامل بنجاح | `/api/whatsapp/session` يعيد حالة العامل وسجل الأحداث؛ لا يوجد 502 من الوكيل. |

## استنتاج مبدئي

لا يوجد فشل في البناء أو في اتصال Vercel بالعامل أو في مسارات API. نقطة الفشل تقع داخل مصافحة Baileys مع WhatsApp على عامل Render. يجب اختبار تغييرات محددة في بروتوكول الجلسة أو طريقة تهيئة المصادقة فقط، مع تجنب طلب إجراءات متكررة من المستخدم حتى تتضح النتيجة.

## التغييرات التي جُربت بالفعل

- تثبيت Node.js 22.
- حفظ بيانات Baileys المشفرة في Neon.
- استعادة Buffers من مخزن Neon دون تحويل مكرر.
- تعريف `Browsers.ubuntu("Chrome")` ومهلة اتصال 60 ثانية.
- استخدام `makeCacheableSignalKeyStore`.
- حفظ كود الربط بعد إغلاق الجلسة.

## نتائج البحث الخارجي

1. يوثق Baileys أن رمز 401 يعني إغلاق جلسة المصادقة، لكن تقارير حديثة تبين أن `7.0.0-rc14` قد يفشل بعد مصافحة أو إعادة ربط حتى مع بيانات اعتماد محفوظة.
2. تقرير Baileys رقم 2248 يذكر اختبار ضبط بصمة المتصفح والإصدار دون حل، ثم يقدم **اختبارًا إيجابيًا**: الإصدار `6.7.23` أكمل مسار QR وإعادة الاتصال على البيئة ذاتها، بينما `7.0.0-rc14` انتهى بـ401 بعد إعادة الربط.
3. تقرير Baileys رقم 2370 يصف مشكلة مصادقة جديدة في إصدارات متعددة ويشير إلى تثبيت إصدار WhatsApp Web يدويًا كحل مؤقت، لكنه ليس مصدرًا رسميًا ولا يُعتمد من دون اختبار منضبط.
4. التوثيق الرسمي يوصي باستخدام مخزن مفاتيح قابل للتخزين المؤقت وحفظ تحديثات بيانات الاعتماد عند كل `creds.update`؛ طُبق ذلك بالفعل.
5. النتيجة الأقوى: تقرير 2248 يطابق المكتبة الحالية `7.0.0-rc14` ويذكر نجاح اختبار تحكم بالإصدار `6.7.23` على البيئة نفسها. إصدار 6.7.23 يعتمد رسميًا على `libsignal` عبر مستودع GitHub، وسياسة الحزم الحالية تمنع هذه التبعية الغريبة تلقائيًا؛ لا يجوز تجاوز المنع دون موافقة المستخدم.

## المصادر

- https://github.com/WhiskeySockets/Baileys/issues/2248
- https://github.com/WhiskeySockets/Baileys/issues/2370
- https://baileys.wiki/advanced/troubleshooting
- https://baileys.wiki/concepts/socket-config
